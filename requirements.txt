# Python 3.13 pre-built wheels (must come before gradio pulls it in)
pillow>=11.0.0

# LLM — Google Gemini 2.0 Flash (free tier: 1,500 req/day)
google-genai>=1.0.0

# Web framework (used by FastAPI routes; gradio adds its own uvicorn)
fastapi==0.115.0
uvicorn[standard]==0.30.0

# Data models
pydantic==2.9.0
pydantic-settings==2.5.0

# Async HTTP
httpx==0.27.0

# Utilities
python-dotenv==1.0.1
python-multipart==0.0.12

# Testing
pytest==8.3.0
pytest-asyncio==0.24.0

# --- OPTIONAL: uncomment when deploying with real backends ---
# redis[asyncio]==5.1.0           # session store (falls back to in-memory)
# asyncpg==0.30.0                 # PostgreSQL
# sendgrid==6.11.0                # email
# opentelemetry-api==1.27.0       # tracing
# opentelemetry-sdk==1.27.0
# opentelemetry-exporter-otlp-proto-grpc==1.27.0
# prometheus-client==0.21.0       # metrics
