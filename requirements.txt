# ============================================================
# Intelligent Customer Support Agent — HuggingFace Spaces
# Strategy: only pin what WE own. Shared deps (fastapi, httpx,
# uvicorn, pillow) are managed by gradio/google-genai — let pip
# resolve compatible versions automatically.
# ============================================================

# LLM — Google Gemini 2.0 Flash (free tier: 1,500 req/day)
google-genai>=1.0.0

# Data models — used directly in app/models/
pydantic>=2.0
pydantic-settings>=2.5.0

# Utilities
python-dotenv>=1.0.0
python-multipart>=0.0.9

# --- OPTIONAL: uncomment when deploying with real backends ---
# redis[asyncio]>=5.0          # session store (falls back to in-memory)
# asyncpg>=0.30                # PostgreSQL
# sendgrid>=6.0                # email
# opentelemetry-api>=1.27.0    # tracing
# opentelemetry-sdk>=1.27.0
# prometheus-client>=0.21.0    # metrics
